import {SkeletonOwnProps} from "@mui/material/Skeleton/Skeleton";

const baseSkeletonProps: Partial<SkeletonOwnProps> = {
  variant: 'rectangular',
  sx: { bgcolor: 'grey.800' }
};

export default baseSkeletonProps;